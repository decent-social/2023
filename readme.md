# decent social website

`npm run build` -> Builds site, and exports to /out
`npm run deploy` -> Builds site, copies to decentsocial.codeberg.page subproject, commits changes, and pushed to DecentSocial/pages repo.

